@model BiteBazaarWeb.Models.Category

@{
    ViewData["Title"] = "Details";
}

<h1>Detaljer</h1>

<div class="text-light">
    <h4>@Html.DisplayFor(model => model.Title)</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>
<div class="text-light">
    <a asp-action="Edit" class="btn btn-egen-blue mb-2 text-light" asp-route-id="@Model?.CategoryId">Spara</a>

    <a asp-action="Category" class="btn btn-outline-black text-decoration-none bg-secondary text-light">Tillbaka</a>
</div>
